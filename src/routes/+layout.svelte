<script lang="ts">
	import '../app.css';

	// onMount(() => {
	// 	// Redirect to default color scheme if at root
	// 	if (window.location.pathname === '/') {
	// 		goto('/profile');
	// 	}
	// });
</script>

<slot />
